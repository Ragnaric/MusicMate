class AuthEndpoints {
  // Mock authentication endpoints for the time being.
  // Might not need if using firebase.
  static String baseUrl = 'base url';

  static String get login => '${baseUrl}login endpoint';
}